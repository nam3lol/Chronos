<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="TopLevelMenuItem" x:Shared="False" TargetType="MenuItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource SecondaryMenuItemForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontFamily" Value="{DynamicResource Poppins}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="6,0,6,0"/>
        <Setter Property="IsCheckable" Value="False"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Grid>
                        <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">

                            <ContentPresenter x:Name="HeaderHost"
                                ContentSource="Header"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                        </Border>

                        <Popup x:Name="PART_Popup"
                               Placement="Bottom"
                               HorizontalOffset="0"
                               VerticalOffset="0"
                               IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                               AllowsTransparency="True"
                               Focusable="False">

                            <Border Background="{DynamicResource PrimaryMenuItemBackgroundBrush}"
                                    BorderBrush="{DynamicResource MenuItemBorderBrush}"
                                    BorderThickness="1"
                                    Padding="2">

                                <ScrollViewer CanContentScroll="True"
                                        VerticalScrollBarVisibility="Auto">
                                    <ItemsPresenter/>
                                </ScrollViewer>

                            </Border>
                        </Popup>

                    </Grid>

                    <ControlTemplate.Triggers>
                        
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background"
                                Value="{DynamicResource MenuItemBackgroundHoverBrush}"/>
                            <Setter Property="Foreground"
                                Value="{DynamicResource MenuItemForegroundHoverBrush}"/>
                            <Setter Property="BorderBrush"
                                Value="{DynamicResource MenuItemBorderHoverBrush}"/>
                        </Trigger>

                        <Trigger Property="IsSubmenuOpen" Value="True">
                            <Setter TargetName="Root" Property="Background"
                                Value="{DynamicResource SecondaryMenuItemBackgroundBrush}"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Opacity" Value="0.5"/>
                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="SubMenuItem" x:Shared="False" TargetType="MenuItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryMenuItemForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontFamily" Value="{DynamicResource Poppins}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="6,2"/>
        <Setter Property="IsCheckable" Value="False"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Grid>
                        <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">

                            <Grid ColumnDefinitions="20,*,Auto,Auto">

                                <Path x:Name="IconPath"
                                    Width="12" Height="12"
                                    Fill="{DynamicResource SecondaryButtonForegroundBrush}"
                                    Stretch="Uniform"
                                    Data="{TemplateBinding Tag}"
                                    VerticalAlignment="Center"
                                    Margin="0,0,6,0"/>

                                <Path x:Name="CheckMark"
                                    Grid.Column="0"
                                    Width="12" Height="12"
                                    Stretch="Uniform"
                                    Fill="{DynamicResource SecondaryButtonForegroundBrush}"
                                    Data="{DynamicResource Check}"
                                    Visibility="Collapsed"
                                    VerticalAlignment="Center"
                                    Margin="0,0,6,0"/>

                                <ContentPresenter x:Name="HeaderHost"
                                    Grid.Column="1"
                                    ContentSource="Header"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>

                                <TextBlock Grid.Column="2"
                                    Text="{TemplateBinding InputGestureText}"
                                    Foreground="{DynamicResource TertiaryMenuItemForegroundBrush}"
                                    Margin="12,0,12,0"
                                    VerticalAlignment="Center"/>

                                <Path x:Name="Arrow"
                                    Grid.Column="3"
                                    Width="10" Height="10"
                                    Stretch="Uniform"
                                    Fill="{DynamicResource SecondaryButtonForegroundBrush}"
                                    Data="{DynamicResource ArrowRight}"
                                    Visibility="Collapsed"
                                    VerticalAlignment="Center"/>
                            </Grid>
                        </Border>

                        <Popup x:Name="PART_Popup"
                            Placement="Bottom"
                            HorizontalOffset="0"
                            VerticalOffset="0"
                            IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                            AllowsTransparency="True"
                            Focusable="False">

                            <Border Background="{DynamicResource PrimaryMenuItemBackgroundBrush}"
                                BorderBrush="{DynamicResource MenuItemBorderBrush}"
                                BorderThickness="1"
                                Padding="2">

                                <ScrollViewer CanContentScroll="True"
                                          VerticalScrollBarVisibility="Auto">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                                
                            </Border>
                        </Popup>

                    </Grid>
                    
                    <ControlTemplate.Triggers>

                        <Trigger Property="Role" Value="TopLevelHeader">
                            <Setter TargetName="HeaderHost" Property="HorizontalAlignment" Value="Center"/>
                            <Setter TargetName="Arrow" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom"/>
                        </Trigger>

                        <Trigger Property="Role" Value="TopLevelItem">
                            <Setter TargetName="HeaderHost" Property="HorizontalAlignment" Value="Center"/>
                            <Setter TargetName="Arrow" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom"/>
                        </Trigger>

                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="HeaderHost" Property="HorizontalAlignment" Value="Left"/>
                            <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="PART_Popup" Property="Placement" Value="Right"/>
                        </Trigger>

                        <Trigger Property="Role" Value="SubmenuItem">
                            <Setter TargetName="HeaderHost" Property="HorizontalAlignment" Value="Left"/>
                            <Setter TargetName="Arrow" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="PART_Popup" Property="Placement" Value="Right"/>
                        </Trigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background"
                            Value="{DynamicResource MenuItemBackgroundHoverBrush}"/>
                            <Setter Property="Foreground"
                            Value="{DynamicResource MenuItemForegroundHoverBrush}"/>
                            <Setter Property="BorderBrush"
                            Value="{DynamicResource MenuItemBorderHoverBrush}"/>
                        </Trigger>

                        <Trigger Property="IsSubmenuOpen" Value="True">
                            <Setter TargetName="Root" Property="Background"
                            Value="{DynamicResource SecondaryMenuItemBackgroundBrush}"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Opacity" Value="0.5"/>
                        </Trigger>

                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="IconPath" Property="Visibility" Value="Collapsed"/>
                        </Trigger>

                        <Trigger Property="Tag" Value="{x:Null}">
                            <Setter TargetName="IconPath" Property="Visibility" Value="Collapsed"/>
                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>